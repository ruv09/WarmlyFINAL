# Standalone package: not part of the root pnpm workspace (Expo 54 /
# React 19.1 must not collide with the root catalog / other Expo apps).
# Install from this directory with:
#   pnpm install --ignore-workspace
